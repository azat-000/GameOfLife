### We could declare deadRow to be final: you may wish to experiment to find out what effect this has on an array. Explain your findings in a brief 1–2 paragraph report.
#### So I would say there is not need to do an expariment because the idea is simple and its more than clear what is going to happen.
#### Explanation: So what final actually do is that it doesn't let us REASSIGN the variable deadRow, which holds nothing else but just a reference of an array. So we can do anything with this: deadRow[i] = anythingWeCouldHaveDoneWIthoutTheFinal. And we would see the change because we modified an element in the reference without changing the reference or giving another reference.